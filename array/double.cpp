#include <iostream>
#include <iomanip> // Include the <iomanip> library for formatting

using namespace std;


int main() {
    // Declare two double variables and assign values to them
    double num1 = 4.0;
    double num2 = 4.0;

    // Calculate the sum
    double sum = num1 + num2;
    

    // Print the sum with decimal point formatting
    cout << "Sum: "  << sum << endl;

    return 0;
}
